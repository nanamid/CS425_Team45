import 'package:flutter/material.dart';

class TaskAvatarView extends StatelessWidget {
  const TaskAvatarView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.lightGreen,
      child: Center(
        child: Text('Task Avatar'),
      ),
    );
  }
}
